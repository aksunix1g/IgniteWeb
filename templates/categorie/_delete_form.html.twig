<form method="post" action="{{ path('categorie_delete', {'categorieproduit': categorie.categorieproduit}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ categorie.categorieproduit) }}">
    <button class="btn">Delete</button>
</form>
